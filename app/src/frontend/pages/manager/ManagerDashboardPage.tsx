import { RoutePlaceholder } from '../../components/RoutePlaceholder';

/** Route skeleton for /manager/dashboard. sp_GetDashboard ships in Phase 2.9. */
export function ManagerDashboardPage() {
  return <RoutePlaceholder title="Tổng quan hệ thống" subtitle="Theo dõi tình hình booking, sân và các chỉ số vận hành chính." icon="dashboard" />;
}