import { RoutePlaceholder } from '../../components/RoutePlaceholder';

/** Route skeleton for /manager/notifications. Notifications ship in Phase 2.6. */
export function ManagerNotificationsPage() {
  return <RoutePlaceholder title="Thông báo" subtitle="Theo dõi các cập nhật liên quan đến booking trong hệ thống." icon="notifications" />;
}