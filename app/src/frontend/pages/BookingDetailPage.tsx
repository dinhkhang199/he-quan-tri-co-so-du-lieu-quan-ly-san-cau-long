import { RoutePlaceholder } from '../components/RoutePlaceholder';

/** Route skeleton for /booking/:courtId. Booking creation ships in Phase 2.4. */
export function BookingDetailPage() {
  return <RoutePlaceholder title="Đặt sân" subtitle="Tạo booking mới cho sân đã chọn." icon="event_available" />;
}