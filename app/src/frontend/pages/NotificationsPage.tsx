import { RoutePlaceholder } from '../components/RoutePlaceholder';

/** Route skeleton for /notifications (customer shell). Notifications ship in Phase 2.6. */
export function NotificationsPage() {
  return <RoutePlaceholder title="Thông báo" subtitle="Theo dõi các cập nhật liên quan đến booking của bạn." icon="notifications" />;
}