import { RoutePlaceholder } from '../components/RoutePlaceholder';

/** Route skeleton for /login. Business login (sp_Login) ships in Phase 2.2. */
export function LoginPage() {
  return <RoutePlaceholder title="Đăng nhập" subtitle="Đăng nhập vào BadmintonPro" icon="lock" />;
}